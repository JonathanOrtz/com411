def directions():
  directions = ["Move foward", 
                "Move Backward",
                "Move left",
                "Move Right"]
  return directions
def run():
  #we call direction to be printed
  # we also can put dirs=direction()
  print(directions())

run()